"""image_generation — optional artifact-generation layer for sentient-futures.

Converts research outputs or prompts into generated images, with full metadata
persistence beside each artifact. Not part of the core scientific pipeline.

Entry points:
  python -m image_generation.cli --help
  python -m image_generation.cli --prompt "..." --name my-image
  python -m image_generation.cli --spec path/to/spec.json
"""

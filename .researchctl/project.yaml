schema_version: 1
project_id: sentient-futures
display_name: Sentient Futures
repo_root: ..
jobs_dir: .researchctl/jobs
staging_root: .researchctl/staging
archive_root: .researchctl/archive
landing_root: artifacts/runs
defaults:
  runtime_class: cpu
  runner_profile_id: local_python
  supports_resume: false
metadata:
  adapter: sentient-futures
  note: "Standalone interpretability scripts wrapped for researchctl-core."

